ALTER TABLE menus
ADD COLUMN show_in_customer BOOLEAN DEFAULT TRUE;




-- Insert data into business_days table
INSERT INTO `business_days` (`day`, `business_id`, `is_weekend`, `created_at`, `updated_at`)
SELECT `day`, `business_id`, `is_weekend`, `created_at`, `updated_at`
FROM `business_times`;

-- Insert data into business_time_slots table
INSERT INTO `business_time_slots` (`business_day_id`, `start_at`, `end_at`, `created_at`, `updated_at`)
SELECT `bd`.`id`, `bt`.`start_at`, `bt`.`end_at`, `bt`.`created_at`, `bt`.`updated_at`
FROM `business_times` AS bt
JOIN `business_days` AS bd
  ON bt.`business_id` = bd.`business_id` AND bt.`day` = bd.`day`;

-- Drop the old business_times table
DROP TABLE IF EXISTS `business_times`;

DROP TABLE `email_templates`;
DROP TABLE `email_template_wrappers`;
DROP TABLE `migrations`;
DROP TABLE `oauth_clients`;
DROP TABLE `oauth_personal_access_clients`;
DROP TABLE `payment_types`;
DROP TABLE `roles`;
DELETE FROM `users`
ORDER BY `id` ASC
LIMIT 1;
UPDATE `users`
SET `email` = 'asjadtariq@gmail.com_test'
WHERE `email` = 'asjadtariq@gmail.com';



ALTER TABLE `orders` ADD COLUMN `discount_type` ENUM('fixed', 'percentage') DEFAULT 'fixed' NULL;



/migrate before
